void main() {
  // print('hhha');

  //  r 前缀可以创建一个 “原始 raw” 字符串 \n 会被直接输出
  // String s = r"In a raw string, even \n isn't special.";
  // int h = 555;
  // print(h);

  // String b = '字符串B';
  // String c = '字符串C';
  // String d = "合并后的${b}和${c}";  // ${}， {} 可以省略。 JS中的 `${}` 用法类式
  // print(d);

  // final date = new DateTime.now();
  // print(date);

  // const date = new DateTime.now();

  // num a = 1;
  // if (a) {
  //   print('JS prints this line.');
  // } else {
  //   print('Dart in production mode prints this line.');
  // }
}
